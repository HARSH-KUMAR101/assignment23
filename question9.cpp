//Write a C++ program to find the maximum of two numbers.
#include<iostream>
using namespace std;
int main()
{ int x,y;
    cout<<"enter two numbers:";
    cin>>x>>y;
      if(x>y)
        cout<<"maximum num is "<<x;
      else
        cout<<"maximum num is "<<y;
  return 0;
}
